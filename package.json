{
  "name": "fcpoly-team-picker",
  "version": "1.0.0",
  "description": "Football team picker with JSON Server backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch db.json --port 3000"
  },
  "keywords": [
    "football",
    "team-picker",
    "json-server"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}

